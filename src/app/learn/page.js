import LearnClient from "@/components/LearnClient";

export const metadata = {
  title: "Build your path — Lumora",
};

export default function LearnPage() {
  return (
    <main className="min-h-screen">
      <LearnClient />
    </main>
  );
}
